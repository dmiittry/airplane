local function spawn_cloud(self)
	for i = 1, 2000 do
		factory.create("#cloudsF", vmath.vector3(math.random(-10000,10000),math.random(-10000,10000),0))
	end
end

function init(self)
	spawn_cloud(self)
end